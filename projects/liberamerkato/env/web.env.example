APP_ENV=production
TZ=America/Argentina/Buenos_Aires

COMPOSER_AUTH={"github-oauth":{"github.com":"ghp_xxx"}}

# La conexión a la base no va acá: la API la lee de config/config.php (que no
# se versiona), apuntando a host 'shared-mysql' en el VPS.
