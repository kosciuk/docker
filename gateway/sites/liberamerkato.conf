MDomain liberamerkato.com www.liberamerkato.com api.liberamerkato.com app.liberamerkato.com img.liberamerkato.com

# liberamerkato.com → www (HTTP)
<VirtualHost *:80>
    ServerName liberamerkato.com
    ServerAlias www.liberamerkato.com
    RewriteEngine On
    RewriteRule ^ https://www.liberamerkato.com%{REQUEST_URI} [R=301,L]
</VirtualHost>

# api.liberamerkato.com (HTTP)
<VirtualHost *:80>
    ServerName api.liberamerkato.com
    RewriteEngine On
    RewriteRule ^ https://api.liberamerkato.com%{REQUEST_URI} [R=301,L]
</VirtualHost>

# app.liberamerkato.com (HTTP)
<VirtualHost *:80>
    ServerName app.liberamerkato.com
    RewriteEngine On
    RewriteRule ^ https://app.liberamerkato.com%{REQUEST_URI} [R=301,L]
</VirtualHost>

# img.liberamerkato.com (HTTP)
<VirtualHost *:80>
    ServerName img.liberamerkato.com
    RewriteEngine On
    RewriteRule ^ https://img.liberamerkato.com%{REQUEST_URI} [R=301,L]
</VirtualHost>

# liberamerkato.com → www (HTTPS)
<VirtualHost *:443>
    ServerName liberamerkato.com
    SSLEngine On
    RewriteEngine On
    RewriteRule ^ https://www.liberamerkato.com%{REQUEST_URI} [R=301,L]
</VirtualHost>

# www.liberamerkato.com (HTTPS) — versión estática SEO
<VirtualHost *:443>
    ServerName www.liberamerkato.com
    SSLEngine On
    ProxyPreserveHost On
    ProxyPass / http://liberamerkato-www/
    ProxyPassReverse / http://liberamerkato-www/
</VirtualHost>

# api.liberamerkato.com (HTTPS)
<VirtualHost *:443>
    ServerName api.liberamerkato.com
    SSLEngine On
    ProxyPreserveHost On
    ProxyPass / http://liberamerkato-api/
    ProxyPassReverse / http://liberamerkato-api/
</VirtualHost>

# app.liberamerkato.com (HTTPS) — SPA Vue (dist)
<VirtualHost *:443>
    ServerName app.liberamerkato.com
    SSLEngine On
    ProxyPreserveHost On
    ProxyPass / http://liberamerkato-app/
    ProxyPassReverse / http://liberamerkato-app/
</VirtualHost>

# img.liberamerkato.com (HTTPS) — uploads
<VirtualHost *:443>
    ServerName img.liberamerkato.com
    SSLEngine On
    ProxyPreserveHost On
    ProxyPass / http://liberamerkato-img/
    ProxyPassReverse / http://liberamerkato-img/
</VirtualHost>
